# There are files generated by the training model.

model

├─ saved_model

│   └── *

└─ checkpoint

└─ frozen_inference_graph.pb

└─ model.ckpt.data-00000-of-00001

└─ model.ckpt.index

└─ model.ckpt.meta

└─ pipeline.config

